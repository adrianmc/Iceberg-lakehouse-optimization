# Dependencias para los jobs de mantenimiento en CDE
#
# maintenance_job.py   -> solo requiere PySpark (provisto por el runtime de CDE)
# invalidate_metadata.py -> requiere impyla + thrift-sasl para transporte HTTP/LDAP

impyla>=0.19.0
thrift-sasl>=0.4.3
thrift>=0.16.0
